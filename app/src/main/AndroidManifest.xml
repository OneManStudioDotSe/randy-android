<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="se.onemanstudio.randy.OneManApplication"
        android:allowBackup="true"
        android:label="Randy"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/OneManTheme"
        tools:targetApi="tiramisu">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="Gen Art"
            android:theme="@style/OneManTheme.BrandingSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".old.BreakoutGameActivity"
            android:exported="true"
            android:theme="@style/Theme.BreakoutGame">

        </activity>
    </application>

</manifest>